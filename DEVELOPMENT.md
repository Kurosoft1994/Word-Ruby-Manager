## 📝 開発の振り返りとAIプロンプト (Development Summary & AI Prompt)

### 1. 今回の作業の振り返り (Project Summary)

本Word VBAマクロ（RubyManager）は、実務や教育現場での利便性を追求し、処理の正確性と操作性を両立させた設計を行いました。

#### 🌟 主な機能と実装上の工夫
1. **直感的で一貫性のある操作モード**
   - 追加・消去ともに「全指定」「ページ単位」「段落単位」の統一されたルールで設計。
2. **学習者レベルに応じたルビ制御**
   - 入門向け「カタカナへのひらがナルビ」から、基本漢字（JLPT N5/N4レベル等）の自動スキップ機能まで対応。
3. **1文字漢字の対話型選択＆履歴学習**
   - 文脈で読みが変わる単漢字（例:「角」「表」）に対し、画面上でハイライト表示して選択・入力可能。選択結果は自動記憶。
4. **設定およびカスタム辞書の自動保存 (`ruby_config.ini`)**
   - UTF-8対応の `.ini` ファイル処理により、前回設定や学習した単語辞書を自動保存・復元。
5. **UI/UXとコード構造の整理**
   - **コンテキストヘルプ**: 入力画面で `?` や `H` を入力すると詳細ヘルプを表示。
   - **Alt+F8 メニューの整理**: エントリポイント以外を `Private` 化し、マクロ一覧をシンプルに維持。
6. **レイアウト崩れ補正処理**
   - ルビ付与に伴う行間の広がり、テキストボックスの文字あふれを抑える自動補正機能を実装。
7. **文末からの逆順処理（Step -1構造）による位置ズレ防止**
   - ルビ付与・削除による文章長や改ページ位置の変動（Rangeオフセット変化）を防ぐため、文書の末尾（最終ページ／最終段落）から先頭ページに向かって逆順（Step -1）で移動・処理する構造を採用。

---

### 🤖 AI（LLM）を活用した協調開発プロセス

本プロジェクトは、生成AI（LLM）によるコード構築と、人間による仕様定義・課題分析・構造設計（Human-in-the-Loop）の組み合わせによって開発されました。

- **仕様定義・構造分析・検証:** 開発者
- **コード生成・リファクタリング・ドキュメント化:** 生成AI
- **検証環境:** Word for Microsoft 365 (Windows 11)

#### 🔄 課題解決における思考過程（構造転換プロセス）
開発初期、正方向（文頭から文末）の処理において「ルビ挿入による文章長の変化で、後続の文字位置や改ページ位置がリアルタイムに変動する」という不具合が発生しました。

1. **AI初期案の限界（事前解析アプローチ）**  
   AIの初期提案は「各ページの初期位置を関数等で事前解析・データ化し、ルビ振りによって位置が変動しても、あらかじめ特定した位置を追跡・補正しながら正方向に処理する」という設計でした。しかし、位置追跡ロジックが複雑化し、データの一致精度が上がらず、不具合を解消できない状態が継続しました。

2. **事前解析の廃止と逆順処理への変更**  
   複雑化した位置追跡アプローチを中止し、「文書の最後（最終ページ・文末）から先頭に向かって逆順（Step -1）にルビを処理する」方式への転換を決定しました。末尾から処理を進めることで、ルビ追加による位置変化（Rangeオフセットの増減）の影響は常に処理済みの後方領域にのみ留まり、前方にある未処理領域の位置情報には影響を与えないためです。

3. **ロジックの再構築**  
   開発者からの指示に基づき、事前解析処理を排除して逆順移動構造へ再構築した結果、位置ズレが生じない設計が完成しました。

#### 📊 AI開発の評価と注意点
- **メリット:** 構造的なアプローチが定まった後のコード生成やリファクタリングが極めて高速であり、開発期間を大幅に短縮できる点。
- **品質管理:** AIの提案が複雑化（オーバーエンジニアリング）した際、人間側が本質的なアルゴリズムを見直し、シンプルな構造へ方向修正を行うことが不可欠。
- **リスク管理:** 免責事項を明示し、実行前のバックアップ取得を推奨。

#### ⚠️ 免責事項
- 本ツールはAIを活用して開発され、十分な動作確認を行っておりますが、全ての環境および特殊な文書構造での完全な動作を保証するものではありません。
- ご利用の際は、必ず対象文書のバックアップを取得した上で実行してください。

---

### 📜 開発プロンプト (Prompt)

本マクロを一から再構築・再現する際に使用したプロンプトです。同様のツール開発やカスタマイズの参考にしてください。

<details>
<summary>クリックしてプロンプトを表示する</summary>

```text
あなたはMicrosoft Word VBAおよび日本語処理・組版システムに精通したシニアエンジニアです。
Word文書内のルビ（フリガナ）を一括追加・削除・補正する高度な標準モジュールマクロ（.bas）および解説用のREADME.mdを作成してください。

以下の詳細仕様と設計要件をすべて網羅して実装してください。

---

### 【要件定義】

#### 1. モード選択（全6モード）
実行時に入力ダイアログ（InputBox）を表示し、以下のいずれかを選択できるようにしてください。
- 1: 【ルビ追加】すべてに振る（全指定）
- 2: 【ルビ追加】ページの最初に出た単語だけに振る（ページ単位）
- 3: 【ルビ追加】段落の最初に出た単語だけに振る（段落単位）
- 4: 【ルビ消去】すべて消去する
- 5: 【ルビ消去】ページの最初のルビを残して重複を削除する（ページ単位）
- 6: 【ルビ消去】段落の最初のルビを残して重複を削除する（段落単位）

#### 2. 対象レベルと自動スキップ
ルビ追加時、学習レベルを選択可能にしてください。
- 1【入門】: 全漢字 ＋ カタカナ（ひらがなルビ）
- 2【初級】: 基本漢字（JLPT N5レベル約100字）を自動スキップ
- 3【中級】: 応用漢字（JLPT N5+N4レベル約300字）を自動スキップ
- 4【カスタム】: 辞書登録単語のみ

#### 3. 1文字漢字の対話型選択＆履歴学習
- 熟語ではない単漢字の場合、「音読み優先」「訓読み固定」「対話選択」を選べるようにすること。
- 「対話選択」の場合、文章中の該当文字を画面スクロールでハイライトし、選択肢（過去履歴・推測読み）の番号選択または直接入力を受け付けること。
- 選択・入力した結果は自動的にカスタム辞書へ学習・保存され、次回以降最優先で適用されること。

#### 4. 設定および辞書の永続化（ruby_config.ini）
- アクティブ文書と同じフォルダに ruby_config.ini（UTF-8）を自動生成・読み込み・保存すること。
- 前回実行時の選択モード、ルビ比率、レイアウト設定、学習した単語辞書をすべて保存し、ダイアログの初期値として再現すること。

#### 5. レイアウト崩れ防止オプション
- 0: 補正なし / 1: 行間固定 / 2: テキストボックス文字あふれ調整 / 3: ページ境界維持 / 4: 全適用
- 複数指定（例: 12）にも対応すること。

#### 6. UI/UXとコード設計ルール
- Alt+F8のマクロ一覧にはメインプロシージャ（例: RubyManagerSystem_RubyFirst）のみが表示されるよう、その他のサブルーチン・関数はすべて Private で宣言すること。
- すべてのInputBoxダイアログで「?」または「H」が入力された場合、その画面専用の【コンテキスト詳細ヘルプ】（MsgBox）を表示して再入力させる仕組み（InputBoxWithHelp）を実装すること。
- Excelオブジェクト（GetPhonetic）を使用した正確な振り仮名取得を行なうこと。

#### 7. 処理順序（末尾からの逆順移動・処理） 【重要】
- ルビ付与・削除による文章長や改ページ位置のズレ（Rangeオフセット変化）を防ぐため、事前解析や補正処理に頼らず、必ず文書の末尾（最終ページ／最終段落）から先頭ページに向かって逆順（Step -1）で移動・処理するプログラム構造にすること。

---

### 【出力形式】
1. GitHub公開用の README.md
2. 上記の要件を満たす単一のWord VBAモジュールコード（RubyManager.bas）
```

</details>
